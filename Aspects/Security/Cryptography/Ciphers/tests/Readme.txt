Some of the tested ciphers require encryption and signing certificates. 
If they are not present the related tests will fail.
In order to create the certificates run once the batch file CreateCertificates.bat.